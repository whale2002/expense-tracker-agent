# ============================================
# 环境配置（可选）
# ============================================

# 运行环境（development/production）
# production 环境下不会记录 Agent 日志
NODE_ENV=development

# ============================================
# API 配置（必填）
# ============================================

# OpenAI 兼容的 API Key
OPENAI_API_KEY=your-api-key-here

# API Base URL（例如：字节跳动豆包、OpenAI 等）
OPENAI_BASE_URL=https://ark.cn-beijing.volces.com/api/v3

# 模型名称
MODEL_NAME=doubao-seed-1-6-251015

# ============================================
# 飞书多维表格配置（必填）
# ============================================

# 飞书应用 App ID
FEISHU_APP_ID=your-feishu-app-id

# 飞书应用 App Secret
FEISHU_APP_SECRET=your-feishu-app-secret

# 多维表格 App Token（从表格 URL 中获取）
FEISHU_APP_TOKEN=your-bitable-app-token

# 多维表格 Table ID（从表格 URL 中获取）
FEISHU_TABLE_ID=your-table-id

# ============================================
# LangSmith 追踪配置（可选）
# ============================================

# 启用 LangChain 追踪
LANGCHAIN_TRACING_V2=true

# LangSmith API Key
LANGCHAIN_API_KEY=your-langsmith-api-key

# LangSmith 项目名称
LANGCHAIN_PROJECT=expense-tracker-agent

# ============================================
# 使用说明
# ============================================
#
# 1. 字节跳动豆包（推荐）：
#    - OPENAI_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
#    - MODEL_NAME=doubao-seed-1-6-251015
#
# 2. OpenAI：
#    - OPENAI_BASE_URL=https://api.openai.com/v1
#    - MODEL_NAME=gpt-4o-mini
#
# 3. 飞书多维表格配置：
#    1. 在 https://open.feishu.cn/ 创建应用
#    2. 创建多维表格，添加字段：备注、分类、金额、收支、日期
#    3. 从表格 URL 获取 app_token 和 table_id
#    4. 为应用开通多维表格权限（新增记录、查看表格）
#
# 4. 飞书机器人配置（用于服务器模式）：
#    1. 在 https://open.feishu.cn/ 创建自建应用
#    2. 启用机器人功能
#    3. 添加权限：im:message、im:message:send_as_bot、im:message:readonly
#    4. 在事件订阅中启用长连接模式（推荐）或 Webhook 模式
#    5. 订阅事件：im.message.receive_v1
#    6. 发布应用并在飞书中添加到群聊
#
# 5. LangSmith（可选，用于调试）：
#    - 访问 https://smith.langchain.com/ 获取 API Key
#    - 用于追踪 Agent 执行过程，便于调试
